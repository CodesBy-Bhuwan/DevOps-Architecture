---
- name: Update apt cache
  apt:
    update_cache: yes

- name: Install common packages
  apt:
    name:
      - curl
      - wget
      - git
      - vim
      - unzip
      - htop
      - net-tools
    state: present

- name: Set timezone
  timezone:
    name: "{{ timezone }}"
